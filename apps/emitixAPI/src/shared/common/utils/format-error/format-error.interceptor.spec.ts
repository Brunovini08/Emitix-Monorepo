import { FormatErrorInterceptor } from './format-error.interceptor';

describe('FormatErrorInterceptor', () => {
  it('should be defined', () => {
    expect(new FormatErrorInterceptor()).toBeDefined();
  });
});
