import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: 'rgb(16 185 129)'
    },
    secondary: {
      main: '#000000'
    }
  }
})

export default theme